pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "mvn compile"
            }
        }
        stage('Test') {
            steps {
                sh "mvn test"
            }
        stage('API Testing with Newman') {
            steps {
                sh " "
            }
        }
            post {
                always {
                    junit '**/TEST-FooTest.xml'
                }
            }
        }
    }
}